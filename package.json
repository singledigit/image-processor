{
  "name": "hello_world",
  "version": "1.0.0",
  "description": "hello world sample for NodeJS",
  "main": "src/index.js",
  "repository": "https://github.com/awslabs/aws-sam-cli/tree/develop/samcli/local/init/templates/cookiecutter-aws-sam-hello-nodejs",
  "author": "SAM CLI",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.18.0"
  },
  "scripts": {
    "package": "sam package --template-file template.yaml --output-template-file out.yaml --s3-bucket ej-sar-apps",
    "deploy": "sam deploy --template-file out.yaml --stack-name image-processor --capabilities CAPABILITY_IAM",
    "release": "npm run package && npm run deploy",
    "test": "mocha tests/unit/"
  },
  "devDependencies": {
    "aws-sdk": "^2.350.0",
    "chai": "^4.1.2",
    "mocha": "^5.1.1"
  }
}
